import 'package:flutter/material.dart';

class adminRouteName extends StatelessWidget {
  const adminRouteName({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}